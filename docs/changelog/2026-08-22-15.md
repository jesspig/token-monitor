# 2026-08-22 15:00–16:00

本轮为第三轮迭代的文档同步修订（代码已实现并通过全量 typecheck + 222 项单测），受影响概念页 frontmatter timestamp 统一刷新至 2026-08-22 15:30。

## 数据质量治理

- 全零拦截：`collector.ts` 新增 `isAllZeroUsage` 统一过滤——解析产物在费用计算前拦截 input/output/cache_read/cache_creation 四项全 0 的记录不入库（游标仍按 nextLine 正常推进）→ `concepts/data-flow.md`（链路图 + 机制 5 改写为「全零过滤与费用计算」）、`concepts/sync-mechanism.md`（去重策略节新增质量闸门条目）。
- v3 迁移：`services/db.ts` 新增一次性清洗——DELETE 存量四项全 0 明细，对受影响日期清空 `usage_daily_rollups` 后按剩余明细以与 recordUsage 一致的口径重建；幂等；`sync_cursors` 不动 → `concepts/data-model.md` 新增「v3 数据修复迁移」专节、note 升级至 v3、`concepts/architecture.md` services 树 db.ts 行。

## 定价自动化与只读化

- 同步策略改为全自动无开关：`host.ts` 的 models.dev 同步间隔常量改 `300_000ms`（每 5 分钟），`startPricingAutoSync` 无条件注册调度（不再读设置），启动序列 seed 后立即执行一次全量同步（seed 仅作离线兜底，启动即被权威 sync 价覆盖）→ `concepts/pricing.md`「models.dev 自动同步」节重写（删除 autoSyncPricing 开关与「每日同步」旧说法）、seed 清单节补兜底定位、零成本回填触发时机更新为「启动 + 每次 models.dev 同步」。
- 定价 UI 只读化：SettingsPage 删除「自动同步定价」区块；PricingPage 移除手动改价表单、行内编辑/删除按钮、在线目录浏览与勾选导入，仅保留只读列表（含来源列）+「立即全量同步」→ `concepts/ui-pages.md` 页面清单、`concepts/architecture.md` 渲染层描述。
- source 三态语义修正：IPC 手动写价入口已下线，`user` 档现为存量行保护标记（v2 迁移保守标记），分级覆盖保护规则不变 → `concepts/pricing.md`「定价表」表。

## IPC 收窄

- 删除 pricing:update / pricing:delete / pricing:modelsdev-import / pricing:modelsdev-catalog 四通道（shared/ipc.ts、preload、register.ts、渲染层封装、mock.ts 全链路），保留 pricing:list 与 pricing:modelsdev-sync；RendererApi 契约 **21 → 17 方法**（ping + 8 查询 + 2 定价 + 2 插件 + 2 设置 + 1 预算 + 1 事件推送）；shared/query.ts 死类型 ModelsDevCatalogResult/ModelsDevImportResult 删除 → `concepts/architecture.md` 分层树与模块职责表、`concepts/ui-pages.md` note；关键约束中「IPC 更新/删除定价后失效缓存」改写为唯一写入路径（models.dev 同步）内置失效。

## CLI 版本探测

- 新增 `services/cli-version.ts`：execFile `<cli> --version`（3000ms 超时；win32 先经 where.exe 定位），失败返回 null，按命令名进程级缓存（clearCliVersionCache 可清空），executor 可注入；`collector.getPluginStatus` 并行探测并以可选字段 `PluginStatus.cliVersion` 返回；shared/dto.ts Detection 增加可选 cliVersion；SourcesPage「版本」行改为「CLI 版本」（cliVersion ?? '未知'）→ `concepts/monitor-plugins.md` 接口注释澄清 version=适配器版本并新增「CLI 版本探测」专节、`concepts/ui-pages.md` 页面清单与交互要点。

## 渲染端体验

- QueryClient 全局默认 `refetchInterval: 5000` 兜底轮询（窗口失焦自动暂停）；useUsageEvents 失效清单补充 budget-status（5 → 6 个 queryKey）→ `concepts/ui-pages.md`「实时刷新」节重写、`concepts/sync-mechanism.md` 实时刷新节。
- App.tsx 外层 h-screen overflow-hidden、内容列 min-h-0 flex-col、main 为唯一 overflow-y-auto 滚动容器（左侧导航固定不随页面滚动）；index.css 追加全局深色滚动条样式（WebKit + Firefox 兜底）→ `concepts/ui-pages.md` 新增「布局与样式」节。

## 知识库全局

- `index.md`：头部摘要补第三轮迭代要点，单测数更新（206 → 222）；pricing 目录摘要补自动同步。
- `overview.md`：note 补第三轮迭代概述并刷新状态数字。
- 本轮未改动：`plugin-architecture.md` / `roadmap.md`（核查后无与新实现矛盾的表述，亦无可勾除待办）。
