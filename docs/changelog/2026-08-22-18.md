# 2026-08-22 18:00–19:00

本轮为第四轮迭代（统计页精简 + 时间范围五档 + 设置拆分）的知识库同步，代码变更已先行完成并验证（typecheck 通过 + 222 用例全绿），本时段仅文档改动。

## 变更一：统计页「按模型」视图移除「应用」列

- 应用维度已有独立 tab，「按模型」表格中的应用列冗余；表格最小宽度 880 → 800px。「按应用」视图不变。
- 知识库位置：`concepts/ui-pages.md` 页面清单「统计」行。

## 变更二：时间范围扩为五档

- RangeKey 扩展为 today / 24h / 7d / 14d / 30d（`lib/range.ts` RANGE_OPTIONS 顺序即此）；today = 本地今天 0 点到现在，24h = 最近 24 小时滚动窗口。
- 趋势页与仪表盘的时间粒度分支改为：today 与 24h 走小时聚合（后端 `usage:hourly-trends`），其余范围走天聚合；后端 LogFilters 为纯时间戳过滤，无后端改动。
- 知识库位置：`concepts/ui-pages.md` 页面清单 Dashboard/趋势两行、「交互要点」时间范围与粒度两条。

## 变更三：设置拆分（刷新间隔与价格同步间隔可配）

- AppSettings 新增 `statsRefreshIntervalMs`（统计自动刷新间隔 ms，默认 5000）与 `pricingSyncIntervalMs`（models.dev 价格自动同步间隔 ms，默认 300_000）。
- 主进程：`startPricingAutoSync` 改从设置读间隔，`updateSettings` 中该值变化时重启调度；旧硬编码常量 `PRICING_AUTO_SYNC_INTERVAL_MS` 删除。
- 渲染端：QueryClient `refetchInterval` 改函数形式，经新增 `lib/settings-cache.ts` 动态读取（useSettings 填充缓存、保存设置后立即生效）；SettingsPage 新增「统计自动刷新间隔（秒）/ 价格同步间隔（分钟）」两输入项。
- 知识库位置：`concepts/pricing.md`（同步节重写为「无启停开关，间隔可配」）、`concepts/architecture.md`（宿主编排钩子）、`concepts/sync-mechanism.md` 与 `concepts/ui-pages.md`（兜底轮询语义）、`concepts/overview.md` / `index.md` / 本文件与 `log.md`（迭代概览）。

## 知识库同步说明

- 概念页修订 5 页并刷新 frontmatter timestamp 至 2026-08-22T18:15:00+08:00：ui-pages / pricing / architecture / sync-mechanism / overview；另 data-flow.md 顺手修正机制 7 的 queryKey 数量（上一轮已补 budget-status 至 6 个，该页漏更）。
- 删除被取代描述：「每 5 分钟无条件自动同步（不读设置）」、「PRICING_AUTO_SYNC_INTERVAL_MS=300000 常量」、「全局默认 refetchInterval: 5000」、「今日 / 7 天 / 30 天三档筛选」、「按模型聚合表含应用维度」。
- 无新增 `[!todo]`（本轮变更均已核实落地）。
