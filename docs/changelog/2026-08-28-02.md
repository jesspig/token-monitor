# 2026-08-28 代码注释移除与知识库一致性对齐

> 按小时合并记录。本条目为文档侧同步；代码侧「移除全部代码注释」见仓库工作区改动。

## 背景
- 本分支已执行「移除全部代码注释」：源码（src/、shared/、配置）内所有注释（JSDoc、行内、块注释、测试注释、配置注释）已删除，仅保留 3 处工具链指令注释——`src/preload/index.ts` 的 `// @ts-ignore` 与 `src/renderer/src/env.d.ts` 的两条 `/// <reference`。
- 代码注释移除后，`docs/` 成为唯一事实来源；原有文档中「以 XX 注释为准 / 顶部矩阵注释」等锚点随之失效，须改为指向代码符号。

## 概念页修订（10 个，frontmatter `timestamp` 刷新至 2026-08-28T02:27:00+08:00）
- overview.md：5→8 插件、单测 222→463（2 项历史遗留失败，位于 usageQuery.test.ts 的 getHourlyTrends，与注释移除无关）。
- architecture.md / plugin-architecture.md：目录树补列 pi.ts / zcode.ts / dsh.ts（共 8 插件）。
- monitor-plugins.md / data-flow.md / data-model.md / sync-mechanism.md：SSOT 锚点由「代码注释」改为代码符号（`isIgnoredFailureReason` / `IGNORED_FAILURE_STATUSES` / `ERROR_MESSAGE_MAX_LENGTH` / `UsageRecord` / `RequestStatus` / `buildWhere`）。
- data-flow.md：机制编号由错序（1–7,10,8,9）修正为连续 1–10，worker 卸载置末。
- pricing.md：「五源实际口径」统一为「8 源实际口径」。
- ui-pages.md：设置页补「关闭到托盘（closeToTray，默认 true）」行。
- roadmap.md：补「截至 2026-08-28」当前状态说明。

## 协调文件
- AGENTS.md：插件数 5→8、单测 361→463、计费语义补全 8 源（codex/gemini/grok/zcode=1、claude/opencode/pi/dsh=2）；新增「## 项目知识库」章节，声明代码注释已移除、docs 为唯一事实来源。
- index.md：目录「5 个内置插件」→「8 个」、数据模型描述「五张核心表」→「六张核心表」；首段补 2026-08-28 对齐说明。
- log.md：新增 2026-08-28 条目；按「仅留 7 天」裁剪 2026-08-21。

## 验证
- 代码侧：typecheck 双段通过；经 Electron 内置 Node 跑 vitest 全量 463 用例（461 通过 / 2 历史遗留失败，与注释移除无关）。
- 文档侧：concepts 各页 grep「顶部矩阵 / 以注释为准 / 五源实际口径 / 5 个内置」除历史语境外零命中；互链与 frontmatter 完好。
