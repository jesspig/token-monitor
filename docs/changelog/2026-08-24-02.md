# 2026-08-24 02:00–03:00

## 修复：dsh 插件模型来源错误导致全部条目被跳过

- **用户真实数据诊断**（`~/.dsh/sessions` 全量 120 会话 / 6084 条 `assistant/message` 实测）：`message.model` 字段 **0/6084 存在**，而插件产出条件要求其非空 → dsh 监控完全无数据。真实的模型信息由 **`request/header` 事件的 `data.header.config.model`** 携带（每步 dispatch 前写入日志），usage 则 100% 存在于 `data.usage`。
- **修复**（与 codex 的 turn_context 状态机同模式）：parseFile 维护 currentModel 状态——遇 `request/header` 行宽松取 `data.header.config.model` 更新；`assistant/message` 模型改为两级来源（message.model 非空优先，否则 currentModel），两者皆无才跳过；续读窗口越过请求头时条目被跳过且水位照常推进（JSDoc 注明边界）。
- **真实数据端到端验证**：修复后逻辑对全部 120 个会话产出 **6084/6084、0 跳过**；模型分布 deepseek-v4-flash ×6083（命中 seed 定价）+ x-preview-f-free ×1。
- 测试同步：fixture 改为真实形态（message 不带 model，新增 request/header 行供模）；新增用例矩阵 a-d（请求头供模 / message 自带优先 / 无请求头跳过 / 连续请求头切换生效）；修复三处行号断言计算错误。vitest **349/349 全绿**（346→净增 3），typecheck / build 通过。
- 文档：monitor-plugins.md dsh 行更新模型来源描述并刷新 timestamp 至 2026-08-24T02:22:00+08:00。
