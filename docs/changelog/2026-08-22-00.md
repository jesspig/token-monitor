# 2026-08-22 00:00–01:00

## AGENTS.md 精简重写

- 就地改进为紧凑指令文件：删除低信号内容（资产清单、技术栈全表——见 docs/concepts/overview.md）；合并冗余章节。
- 补充本日实证的环境坑：pnpm 11 脚本前自动依赖校验安装，非 TTY 终端需先设 `$env:CI='true'`；单文件测试命令示例；验证闭环 = typecheck + vitest（无 lint/format 配置）。
- 核实依据：package.json（无 test 脚本、electron-builder 内联配置产物在 `release/`）、pnpm-workspace.yaml（单包 + allowBuilds）、electron.vite.config.ts（仅 `@renderer` 别名且实际未使用，未写入）、根目录缓存目录与 .gitignore。
