# 2026-09-10 03:00

## 第一梯队 14 监控数据源落地（内置插件 8 → 22）

### 动机

- 将监控覆盖面扩展至第二批 14 个数据源（workbuddy / codebuddy / cline / roo-code / kilo-code / qwen / qoder / qoder-cn / kimi / zed / kiro / reasonix / command-code / copilot-chat），全部经上游源码核实或本机真实文件实地核验后实现；`src/main/plugins/` 插件文件与类型/DB/定价层同轮落地，知识库同步校准为已实现状态。

### 变更

- **基线预登记 + v12 迁移（先行落地）**：`shared/app.ts` 的 `AppType` 联合、`src/main/services/cli-version.ts` 的 `CLI_VERSION_COMMANDS`、`src/renderer/src/lib/format.ts` 的 `APP_META` 各补齐 14 键；db **v12 迁移** DROP 重建 `idx_usage_records_cached_input` 部分索引——semantics=1 组由三源（codex/gemini/grok）扩为**七源**（+workbuddy/codebuddy/qwen/reasonix）；`pricing.recalcCachedInputCosts` 候选 SELECT 同步扩为七源。
- **11 个插件任务产出 14 源（新增 14 个插件文件，内置插件总数 22）**：cline 系三源（cline/roo-code/kilo-code）共享 cline 导出内核（ui_messages.json 整体 JSON 数组 + `api_req_started` text 字符串化用量，全量重析 + requestId=String(ts) 幂等，占位条目游标归零等待回填，model 三级来源，roo/kilo 壳仅换 extensionId 与环境变量）；腾讯系双源（workbuddy/codebuddy）共享 `_lib/tencent-buddy.ts`（双形态 usage，inputTokens 保留含缓存原始量 semantics=1，同 requestId 按 total 大者折叠）；qoder 系双源（qoder/qoder-cn）共享 `_lib/qoder-shared.ts`（明文 SQLite `chat_message` 表，rowid 游标 readonly 打开，semantics=0）；qwen（usage 月度 JSONL，逐 API 响应一行）/ kimi（wire.jsonl 仅 `usage.record`+`usageScope=="turn"`）/ zed（threads.db zstd 列解压，双水位游标 line_offset=rowid + byte_offset=updated_at 毫秒）/ kiro（枚举游标挂 .jsonl、解析读伴生 sidecar）/ reasonix（stats 日文件）/ command-code（v3 类型化流，跳过 checkpoints，叶链回溯活跃分支）/ copilot-chat（v3 patch 流双位置，patch 演进取终值）各自独立插件；`host.ts` 的 `BUILTIN_PLUGINS` 登记至 22 项，各插件均配单测。
- **两项关键调研修正**：
  - **qwen / reasonix semantics=1 经上游源码确证**：qwen `tokenUsageService.ts` 的 promptTokenCount（Gemini 语义含 cached）与 OpenAI converter 的 prompt_tokens（含 cached_tokens）证实 `inputTokens` 已含 `cachedTokens`；reasonix 官方 run_metrics.go 费用公式仅对 cache_hit / cache_miss / completion 三桶计价，证实 `prompt = cache_hit + cache_miss` 为含缓存总量——两源由此纳入 v12 七源索引与缓存扣减计费。
  - **kiro 实现期修正**：预登记假设 token 字段在 `.jsonl` 行内，实地调研证实 **`.jsonl` 行内无 token 字段**，usage 唯一落点为伴生 `<session>.json` sidecar 的 `session_state.conversation_metadata.user_turn_metadatas[]`；仅统计 explicit 实测值，当前社区实测服务端下发恒 0、记录稀少属预期——项目决策禁止推算，不做任何估算回退，待真实数据回填验证。
- **知识库双窗口登记**：`concepts/monitor-plugins.md` 校准为 22 插件已实现状态（14 源小节按实现要点重写、semantics 三态分配汇总、失败判定注明覆盖首批 8 源）；`concepts/data-model.md` 追加 v12 迁移章节并更新 22 源 semantics 分配；`concepts/pricing.md` 重算适用范围扩为七源并补 semantics 分组；`concepts/ui-pages.md` 监控源页更新为 22 插件（列表动态渲染，页面无代码改动）；`log.md` 当日摘要与本 changelog 登记。

### 验证

- vitest 全量 **840 用例通过**（经 Electron 内置 Node 运行，含 14 个新插件的单测）；`pnpm typecheck` 两段通过；`pnpm build` 通过（main/preload/renderer 三段构建成功）。

### 影响页面

- `concepts/monitor-plugins.md`（timestamp 2026-09-10T05:57:12+08:00）
- `concepts/data-model.md`（timestamp 2026-09-10T05:57:12+08:00）
- `concepts/pricing.md`（timestamp 2026-09-10T05:57:12+08:00）
- `concepts/ui-pages.md`（timestamp 2026-09-10T05:57:12+08:00）
- `log.md`（按天摘要，无 frontmatter timestamp）
