# 2026-08-22 06:00–07:00

本轮为 T0–T11 第二轮功能迭代的文档同步修订（代码已实现并通过 typecheck + 206 项单测），受影响概念页 frontmatter timestamp 统一刷新至 2026-08-22 06:42。

## 查询与数据层（T1/T2/T8）

- 聚合查询切换数据源：`usageQuery.ts` 全部聚合类查询（getUsageSummary/getDailyTrends/getModelStats/getAppStats）优先读 `usage_daily_rollups` 镜像；筛选含 status/project/sessionId/keyword 时回退明细表；明细分页/详情不变 → `concepts/data-model.md`（新增「日聚合查询语义」节）、`concepts/data-flow.md`（查询侧说明）、`concepts/architecture.md`。
- 保留清理接线：`host.ts` 启动延迟 30s 清理一次 + 经 scheduler 以 syncIntervalMs 同间隔周期清理，设置变更联动重启，dispose 可逆；90 天前明细删除后历史趋势因 rollups 保留 → `concepts/data-model.md`「保留策略」由设计态改为已接线。
- 零成本回填：`pricing.backfillZeroCost(db, pricing)` 扫 cost=0/null 明细重算并增量修正 rollup.cost_usd（不变量 rollup ≡ 组内明细和，rollup 行缺失不重建）；触发时机 = 启动、定价 update/delete 后、models.dev 同步/导入后 → `concepts/pricing.md` 新增专节、`concepts/data-flow.md` 机制 8。

## 定价体系 v2 + models.dev 同步（T6/T7）

- 定价表迁移升级至 v2：`model_pricing` 加 source 列（'seed'|'sync'|'user'），分级覆盖——user 行挡住一切非 user 写入；IPC 手动编辑默认 'user' → `concepts/data-model.md` 表清单与 note（v1/v2 迁移）、`concepts/pricing.md`「定价表」。
- seed 清单扩容：seedPricing 以 'seed' 来源播种 **99 个主流模型**（原文档 10 条逐项价格表已删除，清单以源码为准）→ `concepts/pricing.md`。
- models.dev 同步：新增 `services/modelsdev.ts`（fetchCatalog/syncPricing，api.json cost 四档映射，cost 缺档补 0，input/output 缺失丢弃）；同步行标 'sync'，导入行标 'user'；同步后 invalidateCache + 回填 → `concepts/pricing.md` 新增专节、`concepts/architecture.md` services 树。

## 渲染层增强（T3/T4/T9/T10）

- 实时刷新接通：新增 `hooks/useUsageEvents.ts` 订阅 usage-updated，失效 5 个用量 queryKey（usage-summary/daily-trends/request-logs/stats-by-model/stats-by-app）→ `concepts/ui-pages.md` 新增「实时刷新」节、`concepts/sync-mechanism.md`、`concepts/data-flow.md` 机制 7。
- 今日小时桶后端化：新增 getHourlyTrends（本地时区按小时 GROUP BY 明细，不补零），前端删除 pageSize:500 截断分桶 → `concepts/ui-pages.md` 交互要点。
- 筛选暴露：getFilterOptions（distinct model/project 各限 500）+ RequestLogsPage 模型多选下拉与项目下拉 → `concepts/ui-pages.md` 页面清单/交互要点。
- 预算告警：AppSettings.dailyBudgetUsd/monthlyBudgetUsd（null = 未启用）；新增 `services/budget.ts` 只读 rollups 算今日/本月费用与占比（全局维度，严格大于才超限）；DashboardPage 顶部横幅（占比 ≥80% 黄色警告、超限红色）；SettingsPage 两字段表单 → `concepts/ui-pages.md`、`concepts/architecture.md`。

## 插件行为修正（T5）

- grok 插件：listFiles 每轮无条件重建 sessionId→模型映射，summary.json 新增当轮生效 → `concepts/monitor-plugins.md` 内置插件清单 grok 行。

## IPC 与架构描述同步（T11）

- IPC 契约从 15 通道扩至 **21 方法**（新增 hourly-trends / filter-options / modelsdev×3 / budget-status 等）→ `concepts/architecture.md` 分层树与模块职责表（分解：ping + 8 查询 + 3 定价 + 3 models.dev + 2 插件 + 2 设置 + 1 预算 + 1 事件推送）、`concepts/ui-pages.md` note。

## 知识库全局

- `index.md`：头部摘要补记本轮迭代要点与单测数更新（156 → 206）；pricing 目录摘要同步。
- `overview.md`：note 补第二轮迭代概述并刷新状态数字。
- 本轮未改动：`log.md`（现有内容未过期）、`roadmap.md` / `plugin-architecture.md`（无与新实现矛盾的表述）。
