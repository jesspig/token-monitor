# 2026-09-11 03:00

## 第三批 9 监控数据源落地（内置插件 22 → 31）

### 动机

- 持续扩大本地监控覆盖面：对第三批 12 个候选监控源做 P0 联网侦察与接入可行性评估（token 是否本地落盘、格式是否可解析、增量水位是否可行），多数经上游源码级核实后按 GO / NO-GO 分流，落地可接入源并同步外围文档与知识库。

### 变更

- **P0 联网侦察（12 候选）**：9 源 GO 接入（dev-eco / mimo / goose / copilot-cli / gptme / trae-agent / codewhale / droid / minimax）；3 源 NO-GO 排除——cursor-cli 本地无 token 落盘、antigravity token 在未文档化 protobuf blob 中、crush token 列快照覆盖含估算值（估算与实测混排，违反数值可溯源）。
- **基线预登记**：`AppType` / `APP_META` / `CLI_VERSION_COMMANDS` 各补 9 键；renderer `mock.ts` 的 `APP_MODELS` 补 9 键。
- **db v13 迁移**：`idx_usage_records_cached_input` 部分索引 DROP 重建为十源（原 v12 七源 + goose / copilot-cli / trae-agent），`recalcCachedInputCosts` 候选 SQL 同步。
- **opencode 解析内核抽取**：opencode 解析逻辑抽为 `_lib/opencode-shared.ts`，dev-eco / mimo 同构会话库复用（延续 cline 系 / 腾讯系 / qoder 系共享内核模式）。
- **9 插件实现**（各配单测，宽松解析 + 错误兜底，`BUILTIN_PLUGINS` 登记 31 项）：
  - `dev-eco`：华为 HarmonyOS 终端 AI 编程助手，OpenCode 同构会话库经 `_lib/opencode-shared.ts` 复用，semantics=2；
  - `mimo`：小米终端 AI 编程助手，OpenCode 同构会话库经 `_lib/opencode-shared.ts` 复用，semantics=2；
  - `goose`：Block 发起的开源 agent，会话级 SQLite 账本 usage_ledger 列探测（按实际存在列取数），semantics=1，无稳定 requestId；
  - `copilot-cli`：GitHub 官方独立 CLI，会话关闭时落盘用量快照，shutdown 事件 delta 状态机防 resume 双计，semantics=1；
  - `gptme`：开源终端 agent，逐会话 JSONL 双形态 metadata 兼容，semantics=2，无稳定 requestId；
  - `trae-agent`：字节开源 CLI agent，工作目录轨迹文件数组水位增量，provider 行级 semantics（openai 系 provider=1 / anthropic 系=2），无稳定 requestId；
  - `codewhale`：社区开源终端 agent，会话级 JSON 快照水位，总量快照分项缺失 semantics=0；
  - `droid`：Factory AI 的 CLI agent，JSONL 事件流双布局兼容 + message.id 折叠，semantics=2；
  - `minimax`：MiniMax 的 CLI agent，SQLite 双账表 rowid 水位，semantics=2，无稳定 requestId。
- **存量测试机械对齐**：host / cli-version / db / pricing-source / storage 五文件的版本号（v12 → v13）与数量断言（22 → 31）更新。

### 验证

- vitest 全量 **1061 用例**通过（Electron 内置 Node 运行时）；typecheck 两段（`tsconfig.node.json` + `tsconfig.web.json`）通过。
- 数值核对（以代码为准）：BUILTIN_PLUGINS 31（`host.ts`）、schema v13（`services/db.ts`，cached_input 部分索引 IN 列表十源：codex/gemini/grok/workbuddy/codebuddy/qwen/reasonix/goose/copilot-cli/trae-agent）、semantics 分布——1 = codex/gemini/grok/zcode/workbuddy/codebuddy/qwen/reasonix/goose/copilot-cli（10 整体）+ trae-agent 行级（openai 系）；2 = claude/opencode/pi/dsh/cline/roo-code/kilo-code/kimi/zed/command-code/copilot-chat/dev-eco/mimo/gptme/droid/minimax（16 整体）+ trae-agent 行级（anthropic 系）；0 = qoder/qoder-cn/kiro/codewhale（4 源）；requestId 覆盖 31 插件中 25 个（kimi/reasonix/goose/gptme/trae-agent/minimax 例外）。

### 影响页面

- `log.md` / `index.md`（无 frontmatter timestamp，按天/编年摘要同步，统一时间 2026-09-11T03:05:47+08:00）
- 根目录 `AGENTS.md` / `README.md`（docs 外围事实同步，同轮修订）
