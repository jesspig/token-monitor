# 2026-08-19 20:00–21:00

## 知识库与设计定稿

- 建立知识库骨架：`docs/index.md`、`docs/log.md`、`docs/changelog/`。
- 确立**插件化监控架构**（一切皆插件）为设计方向，概念页定稿 10 页：
  - `concepts/overview.md`（project-overview）
  - `concepts/architecture.md`（architecture）
  - `concepts/plugin-architecture.md`（plugin-architecture）
  - `concepts/monitor-plugins.md`（plugin-implementation）
  - `concepts/data-flow.md`（architecture）
  - `concepts/data-model.md`（data-model）
  - `concepts/sync-mechanism.md`（sync-design）
  - `concepts/pricing.md`（pricing-design）
  - `concepts/ui-pages.md`（ui-design）
  - `concepts/roadmap.md`（roadmap）
- 更新 AGENTS.md：确立插件化架构决策（注册表 / 服务容器 ctx / 依赖注入 / 生命周期 / 事件总线），并新增「## 项目知识库」章节。
- 里程碑：M2 = 插件框架内核 + 数据层；M3 首个监控插件端到端；M4 其余内置插件；风险含「插件生命周期」。
- 全部页面标注「规划中」状态；无源码可核实处标 `> [!todo] 待补充`。

## 渲染层基座（T13）

- 落地渲染层应用骨架：`src/renderer/src/` 新增 7 个页面（Dashboard 完整、趋势/请求日志/统计/定价/监控源/设置为数据打通的骨架）+ 共享组件（HeroCard/StatCard/RangeSelector/EmptyState/Card/PageHeader/TrendChart）+ TanStack Query hooks + `lib/` 工具。
- 数据层：`api.ts` 封装 `window.api`（对齐 `shared/ipc.ts` RendererApi 契约），后端未就绪时自动回退 `mock.ts` 内置 Mock（确定性伪随机，汇总/趋势/日志/统计互相一致）；`main.tsx` 挂载 QueryClientProvider，`App.tsx` 改为顶部/侧边导航 + state 路由（不引入 react-router）。
- 未改动 `shared/`、`src/main`、`src/preload` 现有逻辑；未运行任何编译/测试命令。
- 更新 `concepts/ui-pages.md` 状态为「渲染层基座已实现」，刷新 timestamp。

## 一致性审计

- 10 个概念页 frontmatter 合法（`type`/`timestamp` 齐全且一致，timestamp 用真实系统时间）。
- 互链满足要求（每页 ≥1 条出站/入站，实测每页 3–4 条）。
- `index.md`/`log.md` 保留文件无 frontmatter。
- 正文不含参考项目名称与本机绝对路径。

## T4 插件框架内核（代码）

- 落地 `src/main/core/` 内核实现（严格对齐 `shared/plugin.ts`、`shared/context.ts` 契约）：
  - `event-bus.ts`：类型化事件总线，实现 `EventsService`；`usage-updated` 200ms 防抖合并（addedRecords 累加、updatedAt 取最后），`on` 返回 disposer。
  - `registry.ts`：`PluginRegistry`（register/list/get/getMeta/has/enable/disable/isEnabled/remove），内存态元数据与启用状态。
  - `context.ts`：`createPluginContext` 以 Proxy 构建服务容器，未提供服务访问抛错；`isServiceReady` 供依赖校验。
  - `lifecycle.ts`：`LifecycleManager`（mount/unmount/retryDeferred），deps 校验 + 延迟装载队列 + 卸载可逆清理（dispose 与登记的 disposer）。
- 新增 vitest 单测（仅编写，未运行）：`event-bus.test.ts`（防抖合并）、`registry.test.ts`、`lifecycle.test.ts`（deps 校验、dispose 清理）。
- 未改动 `shared/` 与既有源码。

## 三页完善（T16）

- `PricingPage.tsx`：表单补全 `currency` 字段（与 `ModelPricingRow` 全字段对齐）；新增行内「编辑」模式——表格行「编辑」载入表单、编辑时 model_id 只读、「保存修改/取消」切换，保存调用 `api.updateModelPricing`（按 model_id upsert，新增/编辑共用），删除调用 `api.deleteModelPricing`。
- `SourcesPage.tsx`、`SettingsPage.tsx`：核对现有实现已满足 T16 需求（插件卡片列表：名称/版本/启停/可用态+reason/最近同步/错误数/会话目录 + 空态；设置表单读写），无改动。
- 未改动 `shared/`、`src/main`、`src/preload` 与 hooks 导出签名；未运行任何编译/测试命令。
