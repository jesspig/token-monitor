# 2026-09-10 01:00

## 图表与布局四项视觉修正

### 动机

- 用户实测截图反馈四项视觉缺陷：统计页「使用量 Top 10」与「费用占比」两列并排高度差悬殊，右下大片空白；仪表盘折线生硬（折角明显）缺乏平滑感；ECharts 6 新默认主题图例落在绘图区内底部、与柱体/X 轴重叠；`App.tsx` main 上 `w-full` 与 `max-w-6xl` 并存存在解析歧义。

### 变更

- **统计页布局**：「使用量 Top 10」与「费用占比（最近 30 天总占比）」取消两列并排（原 `xl:grid-cols-2`），恢复纵向单列全宽堆叠；成功率/缓存命中/会话成本/每百万 4 紧凑卡两列布局保留（`xl:grid-cols-2`）。
- **仪表盘折线**：请求趋势与 Token 趋势全部系列 `smooth: true` 平滑曲线；请求趋势单系列新增面积渐变填充（emerald 0.3→0，复用页面 areaGradient 工具）。
- **图例位置**：`LEGEND_STYLE` 增加 `top: 0` 固定顶部、`GRID_STYLE.top` 8→36 为图例留位（修正 ECharts 6 新默认主题图例落绘图区内底部与柱/X 轴重叠）；统计页两个堆叠柱图例（7 项模型名）加 `type: 'scroll'` 横向滚动；费用占比 donut 图例保持底部（`top: 'auto'` 覆盖，经 ECharts 源码查证 top 有效值优先于 bottom、'auto' 时 bottom 生效）。
- **版心**：`App.tsx` main 删除冗余 `w-full`（`mx-auto max-w-6xl` 居中限宽语义不变，排除解析歧义）。

### 验证

- `pnpm typecheck` 两段通过；vitest 全量 463 用例通过（Electron 内置 Node 运行）；`pnpm build` 通过（main/preload/renderer 三段构建成功）。

### 影响页面

- `concepts/ui-pages.md` 增量修订（timestamp 刷新至 2026-09-10T01:21:07+08:00）：Dashboard 行补折线平滑与请求趋势面积渐变、统计行修正九卡布局为 Top10+donut 单列全宽并补堆叠柱图例 scroll 顶部、donut 图例保持底部。

## 滚动容器全宽化与定价分页调整

### 动机

- 用户实测反馈双滚动条与版心观感问题：窗口出现 html/body 级与 main 级两条滚动条、版心表现不佳；定价页 50/页分页密度偏高，用户偏好更小分页。

### 变更

- **滚动容器全宽化**：`App.tsx` 的 `<main>` 改为 `min-h-0 flex-1 overflow-y-auto`（全宽滚动，滚动条贴窗口右缘惯例位置），版心 `mx-auto max-w-6xl p-6` 内移一层 div 包裹页面渲染——版心仍居中限宽。
- **窗口级滚动物理禁止**：`index.html` head 内联样式 html/body 增加 `height: 100%; overflow: hidden`，新增 `#root { height: 100% }`——main 成为应用唯一滚动容器，消除 html/body 级第二条滚动条。
- **定价页分页**：`PRICING_PAGE_SIZE` 50 → 15。

### 验证

- `pnpm typecheck` 两段通过；vitest 全量 463 用例通过（Electron 内置 Node 运行）；`pnpm build` 通过（main/preload/renderer 三段构建成功）。

### 影响页面

- `concepts/ui-pages.md` 增量修订（timestamp 刷新至 2026-09-10T01:40:26+08:00）：定价配置行分页密度 50/页→15/页；「布局与样式」更新为 main 全宽滚动（滚动条贴窗口右缘）+ 内层 max-w-6xl 版心居中，并补 html/body/#root 锁 100% 高度且禁止滚动、main 为唯一滚动容器。
