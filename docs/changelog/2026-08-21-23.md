# 2026-08-21 23:00–24:00

## 代码-文档一致性审计（知识库增量维护）

- 复验构建声明：`pnpm typecheck` 通过；`pnpm exec vitest run` **15 文件 / 156 用例全绿**，与文档声称一致。
- 重读核心源码核对全部概念页：`core/{registry,context,lifecycle,event-bus}`、`services/{db,storage,pricing,retention,scheduler,watcher,usageQuery}`、`collector.ts`、`host.ts`、`ipc/register.ts`、`preload/index.ts`、5 个插件模块、`shared/{app,plugin}.ts`。

### 修订的概念页（7 页，timestamp 统一刷新为 2026-08-21T23:41:51+08:00）

- **monitor-plugins.md**：修正 opencode / gemini / grok 三行数据源错误——opencode 实为 `~/.local/share/opencode`（`$OPENCODE_HOME` 覆盖）双源（新版 `opencode.db` SQLite 水位增量 + 旧版 `storage/message|session` JSON），非 `~/.opencode/sessions` JSONL；gemini 实为 `~/.gemini/tmp/<hash>/chats/session-*.json` 单 JSON 对象，非 `~/.gemini/sessions` JSONL；grok 实为 `~/.grok`（`GROK_HOME`）下 `logs/unified.jsonl` + `sessions/**/summary.json` 建 sessionId→模型映射。补齐各插件解析字段、扫描范围与 `input_semantics` 取值；消解「JSONL 字段细节待核实」todo。
- **plugin-architecture.md**：修正注册方式（宿主 `BUILTIN_PLUGINS` 清单登记，非目录扫描）与插件形态（平铺单文件 `<id>.ts`，非独立目录）；开发路径去掉不存在的 manifest.ts；以 lifecycle 实际机制消解「装载/卸载时序待定」todo。
- **architecture.md**：目录树改为实际平铺插件文件；补充 db/retention/collector/host 编排说明；IPC 明确 15 通道构成；关键约束补 pricing 缓存失效。
- **data-flow.md**：去重机制更正为主键幂等去重（`data_source:file_path:line` + `INSERT OR IGNORE`），`dedup_ledger` 标注为已建表未接入；链路首行如实反映三种数据载体（JSONL/JSON/SQLite）；补采集编排细节。
- **sync-mechanism.md**：同步触发补齐三路（watcher 500ms 防抖 / 定时兜底默认 5 分钟可调 / 启动立即同步）；去重策略按实现改写，semantic_id 未接入如实标注并保留针对性 todo。
- **data-model.md**：消解两个 todo（保留天数落地为 90 天且 rollups 永不清理；定价 seed 已落地）；费用口径精确化（字符串存储 + 微美元整数聚合）；dedup_ledger 状态更正；补索引清单。
- **pricing.md**：新增 10 个 seed 模型价格表（USD）；归一化细化为实际 7 步规则 + 短 ID 匹配边界字符策略；消解「清单未落地 / 归一化需配单测」todo。

### 其他

- `ui-pages.md` 补 `resource: src/renderer/src/` 并刷新 timestamp（正文核对无误）。
- 为对应具体源码资产的概念页统一补充 frontmatter `resource` 字段。
- `index.md` 引言追加审计结论；`log.md` 追加今日摘要；AGENTS.md 架构要点同步修正（插件登记方式、保留 90 天等高信号事实）。
