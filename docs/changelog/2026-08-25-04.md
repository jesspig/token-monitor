# 2026-08-25 04 时

## dsh 脏游标死锁修复收尾（数据库 v5 迁移）

### 代码改动（唯一依据）

- `src/main/services/db.ts` 新增 v5 迁移（MIGRATIONS 数组末尾）：`DELETE FROM sync_cursors WHERE file_path LIKE '%\.dsh\sessions%'`，清除 dsh 会话文件脏游标。
- 成因链：dsh 初版适配器模型两级来源在真实数据上全部失效（`data.message.model` 实测全量缺失、request/header 兜底未命中）→ 解析零产出但 collector 照常推进游标至文件末尾；commit 3203648 三级来源修复后，a4bfa4c 引入的 mtime 短路（`meta.fileMtime === file.mtime && lineOffset > 0` 则跳过）导致历史文件永不重析，修复失效。
- 迁移安全性：usage_records 无任何 dsh 行且 dedup_ledger 为空；INSERT OR IGNORE 主键幂等 + 语义去重收敛，重放无重复计数风险。
- `src/main/services/storage.test.ts` 新增 describe('v5 迁移：清除 dsh 脏游标触发全量重析') 两用例（清除+保留他源游标；回拨幂等）；storage.test.ts 与 pricing-source.test.ts 的 user_version 断言 4 → 5。

### 验证结果

- 迁移后应用两轮启动完成全量重析：120/120 个 dsh 会话文件游标回写，usage_records 入库 6084 条 dsh 记录（与上游 assistant/message 总数精确吻合），dedup_ledger 同步 6084 条，模型 deepseek-v4-flash 全部命中定价带费用；claude/codex/opencode/zcode 数据完好。
- typecheck 通过；vitest 27 文件 / 363 用例全部通过（经 Electron 内置 Node v20.18.3 运行）。

### 文档同步

- `concepts/data-model.md`：note 迁移链补 v5（升级至 v5）、新增「v5 脏游标清除迁移」专节（成因一句话 + 重放安全 + 实测结果）；刷新 timestamp。
- `concepts/sync-mechanism.md`：mtime 短路条目补「已知风险与自愈」子条（脏游标被短路挡住 → v5 清游标触发全量重析，重放安全靠幂等 + 去重收敛）；刷新 timestamp。
- `concepts/monitor-plugins.md`：note 补 2026-08-25 脏游标收尾一句（初版零产出推满游标 → mtime 短路挡重析 → v5 清游标自愈）；刷新 timestamp。
- `concepts/data-flow.md`：关键机制「增量同步」补脏游标自愈路径一句（v5，dsh 案例）；刷新 timestamp。
- `index.md`：第 3 行追加 2026-08-25 摘要段，「当前 typecheck / 361 项单测」更新为 27 文件 363 用例。
- `log.md`：新增 2026-08-25 条目（最旧条目 2026-08-19 在 7 天内，无需清理）。
