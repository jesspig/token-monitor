# 2026-09-09 23:00

## 死代码清理：移除四个零引用退役组件

### 动机

- 独立趋势页已于 2026-08-29 退役（合并至仪表盘），但 `TrendsPage.tsx` 文件仍残留在 `src/renderer/src/pages/`。
- `TrendChart.tsx` / `DimensionChart.tsx` / `ShareChart.tsx` 三个图表组件经趋势页退役与页面重构后已无任何消费方；其中 `ShareChart` 内的 `DEFAULT_PALETTE` 与 `StatsPage` 的 `PALETTE` 重复，活代码在 `StatsPage` 侧。
- `docs/concepts/ui-pages.md` 的「组件拆分」段仍写「`TrendChart`（保留组件，已去动画）」等现役式表述，与代码事实矛盾。

### 变更

- **删除零引用文件**（均经全仓 grep 查证：ts/tsx/css/json/html 及 kebab-case 路径变体中仅自身文件内出现定义，`App.tsx` 的 `React.lazy` PAGES 表、`NavContext.tsx`、`mock.ts`、`electron.vite.config.ts` 均无引用）：
  - `src/renderer/src/pages/TrendsPage.tsx`
  - `src/renderer/src/components/TrendChart.tsx`
  - `src/renderer/src/components/DimensionChart.tsx`
  - `src/renderer/src/components/ShareChart.tsx`
- **知识库局部修订** `docs/concepts/ui-pages.md`（仅限与上述四个文件直接相关的表述，页面功能描述其他段落不动）：
  - frontmatter `timestamp` 刷新至 2026-09-09T23:13:32+08:00。
  - 「当前状态」段：移除「经 DimensionChart 通用组件统一」「TrendsPage 双图复用 DimensionChart 消除重复内联样式」分句，「费用占比 ShareChart」改「费用占比图」，「TrendsPage/RequestLogsPage 消费共享筛选」改「RequestLogsPage 消费共享筛选」。
  - 「页面清单」统计页行：实现文件列移除 `ShareChart.tsx`。
  - 「加载性能」段：移除「`TrendChart` 包 React.memo；`TrendChart` 与」表述，保留「趋势双图全量 `isAnimationActive={false}` 去动画抖动」。
  - 「组件拆分」段：共享组件清单移除 `TrendChart` / `DimensionChart` / `ShareChart` 三个条目。

### 验证

- 静态查证：全仓 grep 确认四个文件删除前零外部引用、删除后无残留引用（含 `useDailyTrends` 等 `trends` 命名活代码不受影响——该 hook 仍被 `DashboardPage` 消费）。
- 编译与测试验证（`pnpm typecheck` + vitest 全量）由主代理执行，本变更未改动任何其他源代码文件。

### 影响页面

- `concepts/ui-pages.md` 已增量修订，`timestamp` 刷新至 2026-09-09T23:13:32+08:00。
